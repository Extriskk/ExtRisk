# Central report store (pipeline branch)

Post-enhancement analysis reports are written here for API consumption. One JSON file per extension (file-based MVP).

Layout (to be created by the store writer):

- `vscode/<publisher>/<name>.json` — or flat `vscode/<safe_id>.json`
- Optional: `index.json` for listing

See `docs/VSCODE_MARKETPLACE_VALIDATION_AND_HIGH_FIDELITY_PLAN.md` §11.
