{
  "name": "chrome-extension-security-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Retire.js is used by the analyzer for VSCode bundled JS vulnerability scanning.",
  "scripts": {
    "postinstall": "node -e \"console.log('Retire.js installed. Run the analyzer with: python src/analyzer.py <id> --vscode')\"",
    "mal-scan": "node tools/npm-mal-scan/bin/scan.js",
    "mal-scan:build": "npm run build --prefix tools/npm-mal-scan"
  },
  "devDependencies": {
    "retire": "^3.0.0"
  }
}
